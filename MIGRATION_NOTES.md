# Инструкция по миграции базы данных

## 1. Добавление типа 'Ноутбук' в таблицу devices

Для работы нового раздела "Список ноутбуков" необходимо добавить тип 'Ноутбук' в ENUM таблицы `devices`.

### Выполните следующую SQL команду в вашей базе данных:

```sql
ALTER TABLE devices MODIFY COLUMN type ENUM('ПК','Сервер','Принтер','Маршрутизатор','Свитч','МФУ','Интерактивная доска','Ноутбук','Прочее') NOT NULL;
```

### Или используйте готовый файл миграции:

```bash
mysql -u ваш_пользователь -p adminis_db < migrate_add_notebook_type.sql
```

## 2. Добавление полей в таблицу teachers (сотрудники)

Для работы нового раздела "Сотрудники" необходимо добавить новые поля в таблицу `teachers`.

### Выполните следующую SQL команду в вашей базе данных:

```sql
ALTER TABLE teachers 
ADD COLUMN internal_phone VARCHAR(20) NULL COMMENT 'Внутренний телефон' AFTER full_name,
ADD COLUMN mobile_phone VARCHAR(20) NULL COMMENT 'Мобильный телефон' AFTER internal_phone,
ADD COLUMN room_id INT NULL COMMENT 'Кабинет' AFTER mobile_phone,
ADD COLUMN position VARCHAR(255) NULL COMMENT 'Должность' AFTER room_id,
ADD COLUMN email VARCHAR(255) NULL COMMENT 'Email' AFTER position,
ADD COLUMN comment TEXT NULL COMMENT 'Комментарий' AFTER email,
ADD FOREIGN KEY (room_id) REFERENCES rooms(id) ON DELETE SET NULL;
```

### Или используйте готовый файл миграции:

```bash
mysql -u ваш_пользователь -p adminis_db < migrate_add_employee_fields.sql
```

## Что было сделано:

### Раздел ноутбуков:
1. ✅ Создан новый раздел `/notebooks/` для списка всех ноутбуков (аналогично компьютерам и серверам)
2. ✅ Раздел `/laptops/` переименован в "Учёт ноутбуков" (выдача ноутбуков преподавателям)
3. ✅ Обновлён navbar - теперь два отдельных пункта:
   - 💻 Список ноутбуков (`/notebooks/`)
   - 📋 Учёт ноутбуков (`/laptops/`)

### Раздел сотрудников:
1. ✅ Создан новый раздел `/employees/` для управления сотрудниками
2. ✅ Добавлены поля: внутренний телефон, мобильный телефон, кабинет, должность, email, комментарий
3. ✅ Реализованы страницы: список, добавление, редактирование с фильтрацией
4. ✅ Добавлен пункт "Сотрудники" в navbar

### Исправления:
1. ✅ Исправлена страница документации - теперь корректно отображается контент
2. ✅ Улучшена панель разделов в документации
3. ✅ Обновлена главная страница с новыми разделами

## Примечания:

- Если у вас есть иконки для ноутбуков, поместите их в папку `assets/icons/laptop/`
- Новый раздел ноутбуков использует ту же таблицу `devices`, что и компьютеры/серверы, но с типом 'Ноутбук'
- Редактирование ноутбуков происходит через стандартную страницу редактирования устройств (`/rooms/edit_device.php`)
- Раздел сотрудников использует таблицу `teachers` с расширенными полями
